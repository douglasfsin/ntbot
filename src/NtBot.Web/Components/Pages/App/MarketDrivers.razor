@page "/app/drivers"
@page "/app/drivers/{Symbol}"
@layout AppLayout
@rendermode InteractiveServer

<PageTitle>Trading Intelligence — NTBot</PageTitle>

<TradingIntelligenceWorkspace InitialSymbol="@(Symbol ?? "WIN")"
                              ShowSymbolPicker="true"
                              ShowDdeReplay="false"
                              NavBasePath="/app/drivers"
                              Title="Trading Intelligence"
                              Subtitle="Market Drivers · Confluence · Zonas operacionais" />

@code {
    [Parameter] public string? Symbol { get; set; }
}
