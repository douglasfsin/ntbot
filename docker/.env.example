# Copy to .env and fill in values (never commit .env)

APP_DOMAIN=localhost
ASPNETCORE_ENVIRONMENT=Development

# PostgreSQL (mesmo servidor BarberAI; banco ntquant)
POSTGRES_PASSWORD=change_me
DATABASE_URL=Host=46.225.161.55;Port=5435;Database=ntquant;Username=postgres;Password=change_me

# Redis
REDIS_URL=localhost:6379

# Auth — generate a 256-bit random key for production
JWT_SECRET=dev-jwt-key-minimum-32-characters-long
JWT_ISSUER=NTBot
JWT_AUDIENCE=NTBotUsers

# Stripe
STRIPE_SECRET_KEY=sk_test_
STRIPE_PUBLISHABLE_KEY=pk_test_
STRIPE_WEBHOOK_SECRET=whsec_

# SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Optional market data
FMP_API_KEY=
POLYGON_API_KEY=

# API URLs (Web → Api)
API_BASE_URL=http://localhost:5053
