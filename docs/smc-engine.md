# SMC Engine

Smart Money Concepts — adapter em `SmcScoreProviderAdapter`.

## Estado atual

Análise estrutural básica (momentum de candle + direção). Roadmap: order blocks, FVG, BOS/CHoCH via `ChochStrategy`.

## Score

0–100 integrado ao Confluence Engine (peso 15%).

## UI

Exibido por timeframe no gráfico de regiões do workspace `/app/drivers`.
