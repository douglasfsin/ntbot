# Wyckoff Engine

Reutiliza `WyckoffService` (`NtBot.Api/Services/Wyckoff`) via `WyckoffScoreProviderAdapter`.

## Capacidades

- Fases: acumulação, distribuição, markup, markdown
- Spring / Upthrust
- Divergência de volume
- Níveis estruturais

## Timeframes

5, 15, 30, 60 minutos — regiões Máxima / 50% / Mínima no workspace.

## Score

Convertido de `MarketBias` + `PhaseConfidence` para escala 0–100 no Confluence Engine.
